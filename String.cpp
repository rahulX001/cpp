#include<stdio.h>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    cout << a << b;
    for(int i=0; i<n; i++)
    {
        if(a == b)cout<<"NO\n";
        else cout << "YES\n";
        if(a == n)return 0;
    }
    return 0;
}